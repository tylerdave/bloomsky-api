============
Installation
============

At the command line::

    $ pip install BloomSky-API

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv BloomSky-API
    $ pip install BloomSky-API
